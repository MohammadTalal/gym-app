[build]
  command = "npm run build"
  publish = "dist"

# Single-page-app fallback: serve index.html for client-side routes
# (e.g. /library, /progress) so refreshing a deep link doesn't 404.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
