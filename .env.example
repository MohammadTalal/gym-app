# Supabase backend (optional). Copy this file to `.env.local` and fill in
# the values from your Supabase project → Settings → API.
# If these are left empty, the app runs in local-only mode (this browser only).

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
